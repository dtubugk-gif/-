package il.rikavon.feature.blocker.contact

/** Extras the call screen puts on the app's launch intent so the shell opens a specific screen. */
object DeepLinks {
    const val EXTRA_OPEN = "il.rikavon.open"
    const val OPEN_TALK = "talk"
}
