<?xml version="1.0" encoding="utf-8"?>
<!-- The service only draws the island. It asks for no screen content and listens to a single
     rare event type, so it costs nothing while idle. -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeAnnouncement"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="false"
    android:description="@string/a11y_description"
    android:isAccessibilityTool="false"
    android:notificationTimeout="0"
    android:settingsActivity="io.github.dtubugk.island.MainActivity" />
