# אי דינמי · Dynamic Island לאנדרואיד

אפליקציה שמציירת אי שחור בסגנון האייפון סביב המצלמה הקדמית, עם **א** בפנים.
נבנתה ונבדקה עבור Galaxy (One UI), ועובדת בכל אנדרואיד 9 ומעלה.

## מה היא עושה

- **אי במנוחה**: גלולה שחורה שעוטפת בדיוק את חור המצלמה. המיקום נמדד מהמסך עצמו, לא מנוחש. הטקסט (ברירת מחדל: א) יושב בצד שבוחרים.
- **לחיצה**: האי מתרחב בתנועת קפיץ לכרטיס עם שעה, סוללה, ברכה ותאריך עברי. נסגר לבד אחרי 6 שניות.
- **חיבור מטען**: האי מתרחב לרגע עם "טוען" ואחוז סוללה, כמו באייפון.
- **לחיצה ארוכה**: פותחת את מסך ההגדרות. **החלקה למטה מהאי**: פותחת את ההתראות.
- **התאמה אישית חיה**: טקסט (עד 4 תווים או אימוג׳י), 7 צבעים, צד, רוחב, גובה ומיקום. כל שינוי מופיע מיד על האי האמיתי.
- האי נעלם כשהמסך כבוי ובמצב רוחבי, ומוסתר גם ממגע באותו זמן.

## התקנה (פעם אחת, כ-2 דקות)

1. בטלפון, מורידים את הקובץ:
   **https://github.com/dtubugk-gif/-/releases/download/dynamic-island/dynamic-island.apk**
2. פותחים את הקובץ ומאשרים "התקנה ממקור לא ידוע" לדפדפן או ל"הקבצים שלי".
   ב-One UI 8, אם ההתקנה נחסמת: הגדרות ← אבטחה ופרטיות ← **חוסם אוטומטי** ← כיבוי זמני.
3. פותחים את **אי דינמי** ולוחצים **הפעלת האי** ← אפליקציות מותקנות ← אי דינמי ← הפעלה.
4. אם מופיעה ההודעה **"הגדרה מוגבלת"** (זה צפוי באפליקציה שהותקנה מקובץ):
   הגדרות ← אפליקציות ← אי דינמי ← **⋮** למעלה ← לאפשר הגדרות מוגבלות, ואז חוזרים לשלב 3.

### למה שירות נגישות?

זו הדרך היחידה שבה אפליקציה יכולה לצייר **מעל** שורת הסטטוס. בלעדיה, השעון והאייקונים היו מצוירים על האי ומקלקלים את המראה. גם אפליקציות כמו dynamicSpot עובדות כך.
השירות לא קורא את התוכן שעל המסך (`canRetrieveWindowContent=false`), לא מאזין לאירועים ולא דורש אינטרנט.

## פיתוח

```bash
./gradlew :app:testDebugUnitTest    # בדיקות גיאומטריה ופיזיקת הקפיץ + רינדור מסכים
./gradlew :app:recordPaparazziDebug # עדכון תמונות ההשוואה ב-app/src/test/snapshots
./gradlew :app:assembleRelease      # APK חתום ב-app/build/outputs/apk/release
```

כל push בונה APK ב-GitHub Actions ומפרסם אותו כ-Release.

- `island/IslandService.kt` מאתר את חור המצלמה (`DisplayCutout`) ומחזיק את חלון האי מעל שורת הסטטוס.
- `island/IslandView.kt` מצייר את האי ומפעיל את האנימציות: קפיצים לצורה, דהייה לתוכן.
- `island/IslandGeometry.kt` מחשב את המידות, ומבטיח שהאי תמיד מכסה את המצלמה ושהטקסט לא נוגע בה.
- `ui/` מכיל את מסך ההגדרות (Compose, מימין לשמאל, גופן Rubik).

החתימה (`signing/island.keystore`) נמצאת בריפו בכוונה, כדי שכל בנייה תעדכן את האפליקציה המותקנת במקום. לפני פרסום בחנות צריך להחליף אותה במפתח פרטי.
הגופן Rubik מופץ ברישיון SIL OFL (`FONT-LICENSE-OFL.txt`).
