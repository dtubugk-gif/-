# שגרות סגירה 📱

אפליקציית אנדרואיד בסגנון **"מצבים ושגרות"** — אבל עם טוויסט אחד: הפעולות מופעלות **כשיוצאים מאפליקציה**, לא כשפותחים אותה.

לדוגמה:
> כשאני יוצא מ־**יוטיוב** → החזר את הטלפון ל**צלצול**, פתח את אפליקציית **הלימודים**, והקפץ **תזכורת**: "זמן לחזור ללמוד".

## איך זה עובד

האפליקציה משתמשת ב**שירות נגישות** (Accessibility Service) כדי לזהות מתי החלון שבחזית מתחלף. כשיוצאים מאפליקציה שהגדרת עבורה שגרה — הפעולות שבחרת מופעלות אוטומטית. השירות **לא קורא תוכן מהמסך**, רק את שם החבילה של האפליקציה שבחזית.

היציאה מאושרת ב**שני שלבים**: קודם נרשמת יציאה ממתינה, והיא מופעלת רק אחרי שהחלון החדש החזיק מעמד כ־1.5 שניות והאפליקציה המקורית לא חזרה. כך חלונות זמניים — share sheet, דיאלוג הרשאות, הצצה של רגע לאפליקציה אחרת — לא מפעילים שגרות בטעות.

## הפעולות הנתמכות

| פעולה | מה היא עושה |
|---|---|
| **מצב צליל** | מעבר לשקט / רטט / צלצול |
| **עוצמת מדיה** | קביעת עוצמת המדיה באחוזים |
| **נא לא להפריע** | הפעלה או כיבוי |
| **פתיחת אפליקציה** | פתיחה אוטומטית של אפליקציה אחרת |
| **תזכורת** | הקפצת התראה עם טקסט אישי |
| **בהירות מסך** | קביעת בהירות המסך באחוזים |
| **פאנל Wi-Fi** | פתיחת פאנל ה-Wi-Fi של המערכת |

## הורדה והתקנה של ה-APK

ה-APK נבנה אוטומטית ב-GitHub Actions על כל דחיפה לענף.

1. היכנס ללשונית **Actions** ברפוזיטורי.
2. בחר את ההרצה האחרונה של **Build APK**.
3. גלול למטה ל-**Artifacts** והורד את `app-debug-apk`.
4. חלץ את ה-zip — בפנים יש `app-debug.apk`.
5. העבר את הקובץ לטלפון והתקן אותו (צריך לאשר **"התקנה ממקורות לא ידועים"**).

> זהו APK לשימוש אישי, לא להפצה בחנות.

**חשוב — מעבר לגרסה 1.1:** עד גרסה 1.0 כל בנייה נחתמה במפתח אקראי אחר, ולכן התקנת עדכון מעל גרסה קיימת נכשלה. מגרסה 1.1 החתימה קבועה (מפתח שמור בריפו). **פעם אחת בלבד**: הסר את הגרסה הישנה לפני התקנת 1.1 — ומכאן והלאה עדכונים יתקינו מעל בלי להסיר.

## הפעלה ראשונה — הרשאות

אחרי ההתקנה, פתח את האפליקציה ולחץ על אייקון **ההרשאות** (המגן) בפינה. תראה צ'קליסט:

1. **שירות נגישות (חובה)** — בלעדיו שום שגרה לא תעבוד. הכפתור יוביל אותך למסך הנגישות → "שגרות סגירה" → הפעלה.
2. **התראות** — נדרש לפעולת "תזכורת".
3. **גישת נא לא להפריע** — נדרש למעבר למצב שקט ולפעולת "נא לא להפריע".
4. **שינוי הגדרות מערכת** — נדרש לפעולת "בהירות מסך".
5. **תצוגה מעל אפליקציות** — נדרש לפעולת "פתיחת אפליקציה" (אנדרואיד 10+ חוסם פתיחה מהרקע בלי זה).

צריך רק את ההרשאות של הפעולות שבחרת בפועל.

## שימוש

1. לחץ **"שגרה חדשה"**.
2. בחר את האפליקציה שהיציאה ממנה תפעיל את השגרה.
3. הוסף פעולה אחת או יותר וקבע את הפרמטרים שלהן.
4. שמור. השגרה פעילה מיד (כל עוד שירות הנגישות מופעל).

מסך **היסטוריה** מציג את ההפעלות האחרונות.

## בנייה מקומית (למפתחים)

```bash
./gradlew assembleDebug        # בונה app/build/outputs/apk/debug/app-debug.apk
./gradlew testDebugUnitTest    # מריץ את בדיקות היחידה
```

דרוש JDK 17 ו-Android SDK (compileSdk 34).

## פתרון תקלות — "השירות מופעל אבל השגרות לא רצות"

זו התקלה הנפוצה ביותר, בעיקר ב**סמסונג (One UI)**: המערכת מקפיאה את שירות הנגישות ברקע. המתג בהגדרות נראה דלוק, אבל התהליך מוקפא ולכן שום שגרה לא רצה.

**מסך האבחון** (אייקון הלב בסרגל העליון) מראה בזמן אמת אם השירות באמת מחובר ומה זוהה. השתמש בו כדי לאתר בדיוק איפה נתקע.

תיקון בסמסונג, לפי הסדר:
1. **הרשאות ← ביטול אופטימיזציית סוללה** — אשר.
2. הגדרות המערכת ← **סוללה ← מגבלות שימוש ברקע / אפליקציות במצב שינה** — ודא ש"שגרות סגירה" **לא** נמצאת ברשימת "אפליקציות ישנות" / "ישנות עמוקות".
3. הגדרות ← אפליקציות ← שגרות סגירה ← **סוללה ← ללא הגבלה**.
4. כבה והדלק מחדש את שירות הנגישות (הגדרות נגישות ← שגרות סגירה ← כבוי ← דלוק). זה "מעיר" את השירות.
5. פתח את מסך האבחון, פתח אפליקציה עם שגרה, שהה בה ~2 שניות, צא למסך הבית — ובדוק שהיציאה מופיעה באבחון.

אם באבחון היציאה **כן** מופיעה אבל כתוב "לא נמצאה שגרה" — הכלל כבוי או שהאפליקציה שנבחרה אינה זו שיצאת ממנה. אם כתוב "שגרה הורצה" אבל לא קרה כלום בפועל — חסרה ההרשאה של אותה פעולה (למשל "נא לא להפריע" למצב שקט).

## מגבלות ידועות

- **Wi-Fi / בלוטות'**: מאנדרואיד 10 אי-אפשר לכבות/להדליק ישירות מאפליקציה — לכן הפעולה פותחת את פאנל המערכת במקום.
- **מצב שקט** דורש את הרשאת "נא לא להפריע", אחרת אנדרואיד חוסם את השינוי.

## מבנה הפרויקט

```
app/src/main/java/com/routines/appclose/
├── MainActivity.kt                          # ניווט Compose
├── service/AppExitAccessibilityService.kt   # זיהוי יציאה מאפליקציה
├── engine/ExitDetector.kt                   # לוגיקת הזיהוי (נבדקת ב-unit test)
├── engine/ActionExecutor.kt                 # הרצת הפעולות
├── data/                                     # Room: כללים, פעולות, היסטוריה
├── ui/                                       # מסכי Compose (עברית, RTL)
└── util/                                     # רשימת אפליקציות, עזרי הרשאות
```
