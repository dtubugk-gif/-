# סאונדבורד לשיחות · Call Soundboard

אפליקציית Android שמשמיעה הקלטה או שיר מהטלפון שלך **תוך כדי שיחה**, כך שהצד
השני יכול לשמוע — דרך הרמקול (מסלול אקוסטי). קוד מקור מלא + בנייה אוטומטית של
APK ב-GitHub Actions.

> An Android app that plays a local recording/song **during a phone call** so the
> other party can hear it, via the loudspeaker (acoustic path). Full source +
> automated APK build in GitHub Actions.

---

## ⚠️ קראו לפני הכל — מה אפשר ומה אי אפשר

ב-Android **אי אפשר להזריק אודיו ישירות לתוך שיחה סלולרית** כך שהצד השני ישמע
אותו נקי. זו חסימה מכוונת של מערכת ההפעלה — ה-modem שולט בערוץ השמע של השיחה,
ואין לזה API ציבורי (אפילו הקלטת שיחות נחסמה מ-Android 10).

**מה שהאפליקציה כן עושה:** מנגנת את הקליפ **חזק דרך הרמקול**. כשהשיחה על רמקול,
המיקרופון של הטלפון קולט את הצליל והוא מגיע לצד השני. לכן:

- צריך שהשיחה תהיה **על רמקול** (האפליקציה מנסה להדליק אוטומטית, אבל אם הצד השני
  לא שומע — לחצו ידנית על כפתור **"רמקול"** במסך השיחה).
- **האיכות תלויה במכשיר.** למערכת יש מבטל-הד (AEC) שנועד להסיר בדיוק את הצליל
  שהרמקול של אותו טלפון מפיק — כלומר הוא "נלחם" בקליפ שלנו. לכן הצד השני עלול
  לשמוע חלש/מעוות. זה עובד טוב יותר אם הקליפ מגיע מרמקול חיצוני/מכשיר שני ליד
  המיקרופון. **זו מגבלת פלטפורמה ופיזיקה — לא באג באפליקציה.**
- **גם אתה תשמע** את הקליפ (כי הוא מתנגן ברמקול).

מקורות אודיו: **קבצים והקלטות מקומיים בלבד**. אי אפשר לחלץ אודיו מ-Spotify (מוגן
DRM) או מ-YouTube (מפר תנאי שימוש). ניתן להשמיע אותם רק כאפליקציה נפרדת שרצה
ברקע דרך הרמקול.

---

## איך משיגים את ה-APK

הבנייה רצה אוטומטית ב-GitHub Actions בכל push לענף.

1. פתחו את הריפו ב-GitHub → לשונית **Actions**.
2. בחרו את ההרצה האחרונה של **Build APK** (וי ירוק).
3. גללו ל-**Artifacts** והורידו את `call-soundboard-debug-apk`.
4. חלצו את ה-ZIP — בפנים `app-debug.apk`.

אפשר גם להריץ ידנית: **Actions → Build APK → Run workflow**.

---

## התקנה בטלפון (sideload)

1. העבירו את `app-debug.apk` לטלפון (כבל/דרייב/וואטסאפ לעצמכם).
2. פותחים את הקובץ; אם מבקשים — מאשרים **"התקנת אפליקציות לא ידועות"** למקור.
3. מתקינים ופותחים את "סאונדבורד לשיחות".

זהו debug APK חתום במפתח debug — מתאים ל-sideload אישי, לא לחנות Play.

---

## הרשאות שצריך לתת (בתוך האפליקציה)

- **הצגה מעל אפליקציות אחרות** (`SYSTEM_ALERT_WINDOW`) — כדי שהבועה הצפה תופיע
  מעל מסך השיחה. הקישו על הכפתור באפליקציה → הפעילו למסך.
- **טלפון** (`READ_PHONE_STATE`) — כדי לזהות מתי השיחה מתחילה/מסתיימת (הבועה
  נפתחת ונסגרת אוטומטית). בלי זה הבועה עדיין עובדת, רק סוגרים אותה ידנית.
- **התראות** (Android 13+) — להצגת התראת השירות בזמן שהבועה פעילה.
- **שינוי הגדרות שמע** (`MODIFY_AUDIO_SETTINGS`) — ניתנת אוטומטית, לניסיון הדלקת
  הרמקול.

---

## שימוש

1. הוספת קליפים — שתי דרכים:
   - **"טען מההורדות"** — בפעם הראשונה בוחרים את תיקיית **"הורדות" (Download)** ומאשרים
     גישה (חד-פעמי). האפליקציה סורקת ומוסיפה את כל קבצי האודיו שבתיקייה, ומרעננת
     אוטומטית בכל פתיחה כדי לתפוס הורדות חדשות. ("בחר תיקיית הורדות אחרת" לשינוי.)
   - **"הוסף קובץ בודד"** — בחירת קובץ אחד ידנית מכל מקום בטלפון.
   - **שיתוף** — מכל אפליקציה (מנהל קבצים, נגן, אפליקציית ההורדות) בחרו קובץ אודיו
     → **שתף** → **"סאונדבורד לשיחות"**. הקובץ יועתק לאפליקציה ויתווסף לרשימה.
2. תנו את הרשאת **"הצגה מעל אפליקציות אחרות"** (וכדאי גם **טלפון**).
3. אם רוצים שהבועה תיפתח לבד בכל שיחה — השאירו את המתג **"הצג בועה אוטומטית"**
   דלוק. אחרת הקישו **"הצג בועה צפה"** ידנית.
4. התקשרו למישהו. כשמתחברים — הקישו על הבועה, ואז על הקליפ. לחצו **"רמקול"**
   בשיחה אם הצד השני לא שומע.
5. **"עצור השמעה"** עוצר את הקליפ.

בדיקה מהירה בלי להטריד אף אחד: התקשרו לטלפון שני שברשותכם (או לתא הקולי) ובדקו
מה נשמע בצד השני, וכוונו עוצמה.

---

## בנייה מקומית (למפתחים)

הכי פשוט: פותחים את התיקייה ב-**Android Studio** (Giraffe ומעלה) — הוא מוריד
לבד את ה-Gradle wrapper ואת ה-SDK ובונה.

משורת פקודה (צריך Android SDK מותקן ו-JDK 17):

```bash
# מייצרים את ה-Gradle wrapper פעם אחת (ה-.jar לא נשמר בריפו):
gradle wrapper --gradle-version 8.9
./gradlew assembleDebug
# הפלט: app/build/outputs/apk/debug/app-debug.apk
```

**מטריצת גרסאות:** AGP 8.5.2 · Gradle 8.9 · Kotlin 1.9.24 · JDK 17 ·
compileSdk/targetSdk 34 · minSdk 26.

---

## מבנה הפרויקט

```
app/src/main/
├── java/com/callsoundboard/app/
│   ├── MainActivity.kt              # מסך ראשי: רשימת קליפים, הרשאות, הגדרות
│   ├── model/SoundClip.kt           # מודל קליפ (label + content URI)
│   ├── data/SoundRepository.kt      # שמירת קליפים כ-JSON ב-SharedPreferences
│   ├── data/AppSettings.kt          # מתג "בועה אוטומטית"
│   ├── audio/SoundPlayer.kt         # MediaPlayer: ניגון + מקסום עוצמה
│   ├── audio/SpeakerRouter.kt       # הדלקת רמקול (best-effort)
│   ├── call/PhoneStateReceiver.kt   # מאתחל את השירות כשמתחילה שיחה
│   ├── overlay/OverlayService.kt    # בועה צפה + זיהוי סיום שיחה + השמעה
│   └── ui/SoundAdapter.kt           # מתאם רשימת הקליפים
└── res/                             # פריסות, מחרוזות (עברית), אייקון אדפטיבי (XML)
```

---

## שימוש אחראי

השמעת אודיו לצד השני בשיחה עשויה להיות כפופה לחוקי הקלטה/פרטיות/הטרדה — משתנה
לפי מדינה. השתמשו באפליקציה בהסכמת הצד השני, ולא כדי להטעות, להטריד או להתחזות.
